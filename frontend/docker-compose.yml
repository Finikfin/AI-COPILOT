services:
  web:
    image: ${DOCKER_IMAGE:-frontend-web}:${TAG:-latest}
    restart: always
    ports:
      - "8000:80"
    networks:
      - shop-network

networks:
  shop-network:
    external: true
